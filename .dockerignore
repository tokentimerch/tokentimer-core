# Git
.git
.gitignore

# Node modules (installed during build)
node_modules
*/node_modules
**/node_modules

# Build artifacts
apps/dashboard/dist
apps/dashboard/.vite

# Development files
*.log
npm-debug.log*
.DS_Store
*.swp
*.swo

# IDE
.vscode
.idea
*.sublime-*

# Environment
.env
.env.local
.env.*.local

# Documentation
*.md
!README.md

# Tests
**/__tests__
**/*.test.js
**/*.spec.js
tests/

# Deployment configs (not needed inside images)
k8s/
docker-compose*.yml
*.dev.yml

# Non-essential directories
# scripts/ is dev tooling and stays out of build contexts, except the operator
# commands that must be runnable inside a running container (see Dockerfile.api).
scripts/
!scripts/certops-rotate-signing-key.js
!scripts/load-root-env.js
email-previews/
logs/
reports/
