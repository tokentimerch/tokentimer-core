apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tokentimer.fullname" . }}-config
  labels:
    {{- include "tokentimer.labels" . | nindent 4 }}
data:
  {{- include "tokentimer.configmapData" . | nindent 2 }}
